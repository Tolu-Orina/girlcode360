/**
 * LATER — Admin is out of the 14-day consumer PWA sprint.
 * Placeholder only.
 */
export function App() {
  return (
    <main style={{ fontFamily: "system-ui", padding: "2rem", maxWidth: 480 }}>
      <p
        style={{
          display: "inline-block",
          margin: 0,
          padding: "0.25rem 0.6rem",
          background: "#f0e6ec",
          color: "#5c3d5e",
          fontSize: "0.75rem",
          fontWeight: 700,
          letterSpacing: "0.04em",
        }}
      >
        LATER
      </p>
      <h1 style={{ marginTop: "0.75rem" }}>GirlCode360 Admin</h1>
      <p style={{ color: "#5a4d5c" }}>
        Ops / content / support panel — not in current scope. Consumer PWA lives in{" "}
        <code>apps/web</code>.
      </p>
    </main>
  );
}
