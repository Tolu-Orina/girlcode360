version: 0.2
phases:
  install:
    runtime-versions:
      nodejs: 20
    commands:
      - curl -fsSL https://releases.hashicorp.com/terraform/1.9.8/terraform_1.9.8_linux_amd64.zip -o /tmp/tf.zip
      - unzip -o /tmp/tf.zip -d /usr/local/bin
  build:
    commands:
      # Plan artifact is a secondary input; PrimarySource is the git tree (no tfplan).
      # CodeBuild runs /bin/sh — keep this POSIX (no bash pipefail / ${!var}).
      - |
        set -eu
        PLAN_ROOT=""
        if [ -n "${CODEBUILD_SRC_DIR_TerraformPlanOutputProd:-}" ]; then
          PLAN_ROOT="$CODEBUILD_SRC_DIR_TerraformPlanOutputProd"
        elif [ -n "${CODEBUILD_SRC_DIR_TerraformPlanOutputTest:-}" ]; then
          PLAN_ROOT="$CODEBUILD_SRC_DIR_TerraformPlanOutputTest"
        elif [ -n "${CODEBUILD_SRC_DIR_TerraformPlanOutputDev:-}" ]; then
          PLAN_ROOT="$CODEBUILD_SRC_DIR_TerraformPlanOutputDev"
        fi
        if [ -z "$PLAN_ROOT" ] || [ ! -f "$PLAN_ROOT/infra-web/tfplan" ]; then
          echo "ERROR: tfplan not found in plan artifact"
          env | grep '^CODEBUILD_SRC_DIR' || true
          find /codebuild/output -name tfplan 2>/dev/null || true
          exit 1
        fi
        echo "Using plan artifact at $PLAN_ROOT"
        cp "$PLAN_ROOT/infra-web/tfplan" "$CODEBUILD_SRC_DIR/infra-web/tfplan"
      - cd "$CODEBUILD_SRC_DIR/infra-web"
      - |
        terraform init \
          -backend-config="bucket=girlcode360-tf-state" \
          -backend-config="key=web/${TF_VAR_environment}/terraform.tfstate" \
          -backend-config="region=${AWS_DEFAULT_REGION}" \
          -backend-config="dynamodb_table=girlcode360-tf-lock"
      - terraform apply -auto-approve tfplan
artifacts:
  files:
    - '**/*'
